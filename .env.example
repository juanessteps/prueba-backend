# Puerto en el que corre el servidor
PORT=4000

# Conexión a MongoDB local (funciona con MongoDB Compass)
# Si usas Atlas, reemplaza esta URL por la de tu cluster
MONGO_URL=mongodb://localhost:27017/prueba_backend

# Clave secreta para firmar tokens JWT (debe ser larga y única en producción)
JWT_SECRET=supersecreto_muy_largo_y_unico

# Tiempo de expiración de los JWT
JWT_EXPIRES=7d
